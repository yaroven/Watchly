{
  "name": "watchly",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "api",
    "client"
  ],
  "scripts": {
    "commitlint": "commitlint",
    "docker:dev": "docker compose --project-name watchly -f ./config/dev/docker-compose.yml",
    "dev:up": "yarn docker:dev up -d",
    "dev:down": "yarn docker:dev down",
    "lint:api": "yarn workspace api precommit",
    "lint:client": "yarn workspace client precommit",
    "lint": "yarn lint:api && yarn lint:client",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.0",
    "@commitlint/config-conventional": "^20.3.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2"
  }
}
