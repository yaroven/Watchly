server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: docker-containers
    static_configs:
      - targets: [localhost]
        labels:
          job: container-logs
          __path__: /var/lib/docker/containers/*/*.log

    pipeline_stages:
      # Extract container ID from filename
      - regex:
          expression: '^/var/lib/docker/containers/(?P<container_id>[a-f0-9]+)/(?P<log_file>[a-f0-9\-]+)\.log$'
          source: filename

      # Parse Docker JSON log format (log, stream, time)
      - json:
          expressions:
            log: log
            stream: stream
            time: time

      # Extract stream as label
      - labels:
          stream:

      # Parse Pino JSON logs from the actual log content
      - json:
          expressions:
            level: level
            msg: msg
            time: time
          source: log

      # Extract log level as label if present
      - labels:
          level:

      # Parse Pino timestamp (epoch milliseconds)
      - timestamp:
          format: UnixMs
          source: time
          fallback: time

      # Replace the output with the actual log content
      - output:
          source: log
